dependencies:
  pre:
    - sudo touch ~/ci.txt
    - sudo pip install awscli

test:
  override:
    - bundle exec rake spec

deployment:
  staging:
    branch: master
    commands:
       - sh deploy_staging.sh
